# Explicit version pins for Odoo 17.0 builds.
# Each line replaces the aggregated constraint for that package.
# Use when aggregated constraints produce impossible combinations
# (e.g. an old repo pins <X while a newer one requires >=X).
#
# google-books-api-wrapper used to require pinning idna==3.4 then
# requests==2.28.2 here (see git history) — each pin surfaced a new
# conflict, so the package itself is now excluded in
# aggregate_requirements.py's _SKIP_PACKAGES instead. No override
# needed until something else in the aggregate produces a genuine
# cross-repo version conflict for 17.0.
